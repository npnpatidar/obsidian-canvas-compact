{
  "name": "canvas-compact",
  "version": "2.0.0",
  "description": "Obsidian plugin: clean canvas layout — no card or label overlaps, minimal connection crossings",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "canvas",
    "layout",
    "packing"
  ],
  "author": "npnpatidar",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "typescript": "^5.8.3",
    "@types/node": "^20.0.0"
  }
}
